export enum LoyaltyTransactionType {
  EARN = 'EARN', // Earn (on purchase or as gift)
  SPEND = 'SPEND', // Spend (using points for discount or expired reset)
  TRANSFER = 'TRANSFER', // Transfer (between users)
}
